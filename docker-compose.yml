version: "3.9"

services:
  api:
    build: .
    container_name: api-go-mvp
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DB_PATH=/app/data/api.db
    volumes:
      - ./data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock
